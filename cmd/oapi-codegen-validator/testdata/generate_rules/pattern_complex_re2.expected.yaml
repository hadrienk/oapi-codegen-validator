openapi: 3.0.0
info:
  title: Test
  version: 1.0.0
paths: {}
components:
  schemas:
    TestSchema:
      type: object
      properties:
        field:
          type: string
          pattern: "^[a-z0-9]+(-[a-z0-9]+)*$"
          x-oapi-codegen-extra-tags:
            validate: "omitempty,regex=^[a-z0-9]+(-[a-z0-9]+)*$"
