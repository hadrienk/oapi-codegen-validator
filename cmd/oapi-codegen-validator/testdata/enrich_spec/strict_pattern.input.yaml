openapi: 3.0.0
info:
  title: Test
  version: 1.0.0
paths: {}
components:
  schemas:
    TestSchema:
      type: object
      properties:
        complex:
          type: string
          pattern: "^(?=.*[a-z]).+$"
          x-oapi-codegen-extra-tags:
            validate: manual_override
